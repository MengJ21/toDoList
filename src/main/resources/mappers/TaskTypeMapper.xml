<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meng.todolist.mapper.TaskTypeMapper">
    <select id="findTypeByTypeId" resultType="TaskType">
        select * from task_type where type_id = #{typeId}
    </select>
</mapper>